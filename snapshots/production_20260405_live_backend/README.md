# Production Snapshot

This directory preserves the live production code that existed on 2026-04-05 before the readability rewrite.

It is a file snapshot, not a git branch or tag, because the main worktree had many generated changes.
